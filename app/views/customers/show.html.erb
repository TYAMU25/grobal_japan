<div class="container">
  <div class="row">
    <div class="col-md-4">
      <%#= attachment_image_tag current_general_user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg"%>
      <table class="table">
        <tbody>
          <tr>
            <th>名前</th>
        	  <th><%= current_general_user.name%></th>
          </tr>
          <tr>
        	  <td>フォロー数: <%= current_general_user.followings.count %></td>
        	</tr>
        	<tr>
        	  <td>フォロワー数: <%= current_general_user.followers.count %></td>
          </tr>
        </tbody>
      </table>

      <div class="row">
       <%=link_to"編集" ,edit_customer_path(@current_general_user),class:"btn btn-outline-light btn-block fas fa-user-cog"%>
      </div>
      <td><%#= link_to 'Destroy', submission_path(@submission), method: :delete, data: { confirm: '本当に消しますか？' }, class: "btn btn-sm btn-danger destroy__#{submission_path(@current_general_user).id}"%></td>
        