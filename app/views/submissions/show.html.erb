<div class="col-md-8 offset-md-1">
  <h2>投稿一覧</h2>
  <table class="table table-hover table-inverse">
    <tr>
      <th></th>
      <th>投稿タイトル</th>
      <th colspan="3"></th>
  </table>
</div>
<%= form_for [@submission, @commint] do |f| %>
  <%= f.text_field :comment %>
  <br>
  <%= f.submit 'コメントする' %>
<% end %>

<% @commints.each do |commint| %>
    
 <div><%= commint.comment %></div>
 
<% end %>