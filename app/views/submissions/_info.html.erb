<% if current_general_user.id != user.id %>
<% if current_general_user.following?(user) %>
  <%= link_to "フォロー中", relationships_path(general_user_id: user.id), method: :delete %>
<% else %>
  <%= link_to "フォローする", relationships_path(general_user_id: user.id), method: :post %>
<% end %>
<% end %>