<% if current_general_user.id != user.id %>
<% if current_general_user.following?(user) %>
  <%= link_to "フォロー中", relationships_path(general_user_id: user.id), method: :delete,class:"btn btn-outline-light" %>
<% else %>
  <%= link_to "フォロー", relationships_path(general_user_id: user.id), method: :post,class:"btn btn-light" %>
<% end %>
<% end %>